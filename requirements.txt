fastapi==0.138.0
uvicorn==0.49.0
Jinja2==3.1.6
httpx==0.28.1
python-multipart==0.0.32
numpy==2.5.0
weasyprint==63.1
PyNiteFEA==3.0.0  # solveur EF du moteur de calcul (bascule roadmap etape 1, branche refactor/pynite)

# Authentification (fastapi-users)
fastapi-users[sqlalchemy]
aiosqlite
argon2-cffi
python-dotenv
disposable-email-domains

# Migrations de schéma de base de données
alembic==1.19.2

# Back office admin (session 10)
sqladmin==0.31.1
itsdangerous==2.2.0  # requis par SessionMiddleware (auth SQLAdmin) ; pas tiré auto par sqladmin
# wtforms est tiré automatiquement par sqladmin
